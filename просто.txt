массив-не отдельный тип данных,а разновидость объекта

интерполяция-обращение к значению какой-то пременной:
const name = 'Ann';
 команда(`Привет,${name}`);

префиксная ++i
постфиксная i++
В первом случае возвращается уже результат операции,
а во втором случае возвращается сначала первоначальное число, а потом уже результат.

=== строгое сравнение 

Гит

git status
git add -A (git add main.css (*.css))
git commit -a -m"first commit"

После внесения изменений в проекте 
git add -A
git commit -a -m"second"

Просмотреть коммиты 
git log


на сайте создать новый репозиторий и там второй пункт 2 команды по очереди вставить
добавить коммит на гитхаб:
git push 

обновить в своей другой папке все файлы до последнего изменения:
git pull

